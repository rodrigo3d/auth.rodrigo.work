{
  "name": "auth.rodrigo.work",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "push": "git add . && git commit --amend -m \"chore: update\" && git push -f origin main"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.3.2",
    "turbo": "^2.0.3"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=16"
  }
}
